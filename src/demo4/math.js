import { join, concat, filter } from 'lodash-es'

export function square(x) {
  console.log('*****square******')
  var a = join(['a', 'c'])
  var b = []
  concat([], [])
  b.filter(function (a) { return a })
  return x * x;
}

export function cube(x) {
  console.log('this is cube')
  return x * x * x;
}